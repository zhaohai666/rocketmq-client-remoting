# 2026-09-17

## POP 模式（5.x 轻量消费，最后一个 P2 缺口）—— 调研 + 真机探针 + Python 参考实现完成

### 一、真机探针：单 broker 5.5.1 **原生支持 POP，无需 proxy、无需任何开关**

用 Java 探针（`/tmp/PopProbe.java` + `/tmp/run_pop_probe2.sh`，distribution classpath）实测：

```
code=0 (SUCCESS)  remark=FOUND
popTime=1789613086027 invisibleTime=60000 reviveQid=0 restNum=0
startOffsetInfo=[0 0 0;0 3 0;0 2 0]
msgOffsetInfo  =[0 0 0;0 3 0;0 2 0]
decodedMsgCount=3
```

- `BrokerController` **无条件**注册 POP_MESSAGE(200050) / ACK_MESSAGE(200051) /
  CHANGE_MESSAGE_INVISIBLETIME(200053)（`:1198` / `:1204-1208` / `:1212`），没有 `enablePop` 主开关。
- 唯一硬依赖 `MessageStoreConfig.timerWheelEnable`，**默认 true**；关掉才回 SYSTEM_ERROR。
- `autoCreateSubscriptionGroup=true` 让新消费组首次 POP 自动建订阅组。
- 请求码更正：**200052 是 PEEK，延长不可见时间是 200053**。

### 二、最贵的一个认知纠正：`POP_CK` 是**客户端**写的，不是 broker 写的

- **普通 topic 直连 POP 时消息上没有任何 POP_CK 属性**（探针实测 props 只有
  `MSG_REGION/UNIQ_KEY/CLUSTER/KEYS/WAIT/TRACE_ON`）—— 我差点据此误判"POP 不可用"。
- broker 只在 **retry-topic 重编码路径**才写（`PopMessageProcessor.java:849` 的 `putIfAbsent`）。
- 普通 topic 的 CK 串必须由**客户端** `MQClientAPIImpl.processPopResponse`（`:1150-1225`）
  用响应头的 `startOffsetInfo` / `msgOffsetInfo` 反构：
  按 `topic@queueId` 取 startOffset → 按 queueOffset 在同队列 offset 列表里求 index 取
  msgQueueOffset → `buildExtraInfo(...)` 拼 **8 段**（空格分隔）。**没有它就无法 ACK。**
- 反构完还要补 `1ST_POP_TIME`（仅当缺失，值 = popTime）。
- 查表 key 的坑：`buildQueueOffsetSortedMap` 用的 retryFlag 在"消息已带 POP_CK"时取自
  **POP_CK 第 5 段**，否则由**消息 topic** 推断（broker 可能改写 topic）。

### 三、其他关键协议事实

- `bornTime` 必须填**当前毫秒时间戳**：broker 用 `now - bornTime - pollTime > 500` 判定
  "超时太久"直接回 `POLLING_TIMEOUT(210)`（`PopMessageRequestHeader:135`）。
- `maxMsgNums > 32` → INVALID_PARAMETER；`queueId >= readQueueNums` → INVALID_PARAMETER；
  `queueId = -1` 表示弹所有队列；`initMode` 0=MIN / 非 0=MAX。
- **ACK 的 `offset` 是 consumeQueue offset**（CK 第 8 段），不是 commitlog offset。
  实测非法 queueId → `MESSAGE_ILLEGAL(13)`，越界 offset → `NO_MESSAGE(208)`。
- 响应码映射：SUCCESS→FOUND；POLLING_FULL(209)→POLLING_FULL；
  POLLING_TIMEOUT(210)/PULL_NOT_FOUND(19)→POLLING_NOT_FOUND；其它抛异常。
- `CHANGE_MESSAGE_INVISIBLETIME` 返回**新的** popTime/invisibleTime/reviveQid，
  客户端要用新值重建 extraInfo 再 ACK（Java `MQClientAPIImpl:1025-1028`）。
- 本仓 5.5.1 **不存在** `INVISIBLE_TIME` / `POP_CORRELATION_ID` / `POP_SKIP_ACK` 这些
  属性常量（grep 0 命中），属 gRPC/Proxy 概念，直连 remoting 不要去找。
- 复活：ck 的 `reviveTime = popTime + invisibleTime` 到点且未全 ack → 重投到
  `%RETRY%<group>_<topic>`（V1，`_`；V2 用 `+`，`enableRetryTopicV2` 默认 false）。

### 四、Python 参考实现完成并真机验证（提交 d8c970d，未 push）

- 新增 `remoting/protocol/extra_info.py`（ExtraInfoUtil 移植）、
  `headers.py` 追加 5 个 POP 头、`mq_client.py` 追加
  `pop_message` / `ack_message` / `change_invisible_time`、
  `consumer_result.py` 追加 `PopStatus` / `PopResult` / `ChangeInvisibleTimeResult`、
  `message_const.py` 追加 `PROPERTY_FIRST_POP_TIME`。
- 坑：**Java 的 `String.split(" ")` 丢弃末尾空串，Python 的 `str.split(" ")` 不丢** ——
  段数校验依赖这个差异，必须显式复刻。
- 坑：`order` / `suspend` 这两个字段 Java 用的是非空 `Boolean order = Boolean.FALSE` /
  `private boolean suspend`，`encodeHeader` 只跳 null，所以它们**总是**出现在报文里。
- 坑：`MQClientInstance._invoke_sync` 的请求在 `make_custom_header_to_net()` 之前
  `ext_fields` 是空的 —— 单测断言 extFields 键名时必须显式触发它。
- 单测 `tests/test_pop.py` **50 例**；pytest 全量 **222 passed / 4 skipped**
  （`test_logging_config` 的 2 个 error 是沙箱 tmpdir EEXIST 噪音，非回归）。
- 真机 `python/verify_pop_live.py` S1–S8 **14/14 PASS**（`bash /tmp/run_pop_live.sh python`）。
  最有价值的是 S8：小 invisibleTime POP 后**不 ack**，20s 后重 POP 拿到同 3 条消息，
  且它们的 POP_CK `retryFlag=1` —— 既证明至少一次语义，也证明我们**没有覆盖**
  broker 在 retry 消息上写好的 POP_CK。

### 五、worker 情况（429 限流）

- `general-purpose-7`（真机探针）失败 → **主会话接手**，探针跑通（见上）。
- `general-purpose-10`（Python 实现）失败 → **主会话接手完成**。
- `general-purpose-12`（.NET 实现）失败，**未留下任何文件**。
- `general-purpose-11`（C++ 实现）截至本次记录仍在跑，尚未落盘。
- 限流窗口：2026-09-17 12:17:56 UTC+8 重置。

### 六、下一步

1. C++（`extra_info.h/.cpp` + 5 头 + `popMessage`/`ackMessage`/`changeInvisibleTime` +
   `test_pop.cpp` + `live_pop.cpp`）与 .NET 同理移植，照 Python 为模板。
2. 三侧真机 `bash /tmp/run_pop_live.sh all` 全绿后 push。
3. 之后可考虑**消费侧 POP 轻量消费循环**（在现有 push consumer 上加 POP 模式：
   复用客户端 rebalance，逐队列 POP → 投递 → 成功 ACK、失败不 ACK 交给 broker 复活）。
   注意 Java 的 push-consumer POP 走的是 **broker 侧分配** `QUERY_ASSIGNMENT(400)`
   + `MessageQueueAssignment(mode=POP)`，本项目暂不实现，改用客户端 rebalance 等价路径，
   需在文档里写明这个有意差异。
